# Optional: set a secret to protect the cron endpoint.
# Call: GET /api/cron/send-due-posts?secret=YOUR_SECRET
# or header: X-Cron-Secret: YOUR_SECRET
CRON_SECRET=your-secret-here

# Optional: custom SQLite database path (default: ./data/postplan.db)
# DATABASE_PATH=./data/postplan.db
